ѕараметры `train.py`:

  -h, --help           show this help message and exit
  --model MODEL        путь к файлу, в который сохран€етс€ модель
  --inputdir INPUTDIR  путь к директории, в которой лежит коллекци€ документов.                        
                       ≈сли данный аргумент не задан, тексты ввод€тс€ из stdin.


ѕараметры `generate.py`:

  -h, --help       show this help message and exit
  --model MODEL    путь к файлу, из которого загружаетс€ модель
  --length LENGTH  длина генерируемой последовательности
  --prefix PREFIX  необ€зательный аргумент. Ќачало предложени€ одно или 2 слова. 
                   ≈сли не указано, начальное слово будет выбрано случайно из всех слов.
